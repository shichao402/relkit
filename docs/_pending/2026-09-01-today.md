# 今日待办（2026-09-01）

> 用完即删。只服务今天这条线：去掉运行时 fallback → 上 main → 部署发布机 → 清产品根遗留 json。  
> 经验稿另见 [`2026-09-01-publish-config-lessons.md`](2026-09-01-publish-config-lessons.md)。

## 1. relkit 代码（本机，已部分完成）

- [x] 去掉 `loadProductConfig` 对产品根 `relkit.json` 的 fallback
- [x] `-migrate-profile` 迁完改名为 `relkit.json.migrated`
- [x] 文档：`publish-agent.md` / agent README / CLI / `update-ingress-cos.md`
- [ ] `PUT /v1/staged`：无 `release-policy.json` 直接 400（现在要到 publish 才失败）
- [ ] `go test ./cmd/relkit-agent/` 再跑一遍
- [ ] **在 `main` 提交并推送**（用户已口头同意这条）

## 2. 发布机 `cvm-gz`（`publish.firoyang.com` / `43.138.156.146`）

profile 已存在，**不要再跑** `-migrate-profile`。

- [ ] 装上刚推的 `relkit-agent`，确认版本是新 commit 的完整 SHA
- [ ] `systemctl restart relkit-agent`，`GET /-/health` 通过
- [ ] 手工挪开遗留（已迁过，agent 不再读；留着会成第二真相源）：
  - `/srv/relkit/dec/relkit.json` → `relkit.json.migrated`
  - `/srv/relkit/cronkit/relkit.json` → `relkit.json.migrated`
- [ ] 确认仍在：`/etc/relkit-agent/products/{dec,cronkit}.json`、env、token、私钥

## 3. Dec 验收一版

- [ ] 钉 relkit **完整 SHA**（短 SHA 在 CNB fetch 会失败）
- [ ] 发一版（建议 `dev`）
- [ ] agent 日志必须是 `publish config: staged release-policy.json + .../dec.json`，**不得出现** `legacy`
- [ ] `updates.firoyang.com` 产物 200
- [ ] `update.firoyang.com` 人页版本跟上

## 4. cronkit（可选，人页策略未定）

- [ ] 确认仓库 CI 会打 `release-policy.json`
- [ ] 确认人页要不要跟 Dec 同一套 Makers（旧 json 里 `homepage` 空、无 makers）
- [ ] 真机发一版走通（日志同样不得 `legacy`）

## 5. 收尾

- [ ] 经验稿择要并入正式设计文档（若今天来得及）
- [ ] 删掉本文件
- [ ] 删掉 `2026-09-01-publish-config-lessons.md`（或并入后再删）
