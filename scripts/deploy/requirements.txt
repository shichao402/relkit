# Third-party Python packages for scripts/deploy/relkit.py.
# Keep this file empty of package pins: the CLI is stdlib-only.
# If a pin is added, relkit.py creates scripts/deploy/.venv, pip-installs, and re-execs.
