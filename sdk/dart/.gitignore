# Analyzer and pub build state, regenerated by `dart pub get`.
.dart_tool/
.packages

# Committed on purpose. This is an application-side package rather than one
# published to pub.dev, and pinning the resolved versions is what keeps a
# signature-verifying dependency from changing under a release build without
# anyone noticing.
!pubspec.lock
